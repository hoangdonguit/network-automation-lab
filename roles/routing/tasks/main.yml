---
# Tự động hóa cấu hình định tuyến (Routing Configuration)
- name: ROUTING | Enable OSPF Process 1 (Area 0)
  cisco.ios.ios_config:
    lines:
      # Lệnh này bật OSPF trên tất cả các cổng mạng đang có IP
      - network 0.0.0.0 255.255.255.255 area 0
    parents: router ospf 1