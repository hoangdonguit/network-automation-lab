all:
  children:
    cisco_routers:
      hosts:
        R1:
          ansible_host: 172.17.0.11
        R2:
          ansible_host: 172.17.0.12
      vars:
        ansible_user: admin
        ansible_password: cisco
        ansible_connection: network_cli
        ansible_network_os: ios